# 空コミットを作成するタスク

# 手順
1. 以下の情報が提供されていないければ、ユーザーに質問して提供してもらう
    - コミットメッセージ
2. `git commit --allow-empty -m "<commit_message>"`コマンドで、指定されたコミットメッセージで空コミットを作成してください

# コミットメッセージの形式
[prefix]／[Redmineチケットのタイトル] refs #[チケット番号]
例）外貨対応／金額を入力できるようにする refs #12345
